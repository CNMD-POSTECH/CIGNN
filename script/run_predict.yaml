type : 'predict'                  # Mode to run: 'train' or 'predict'
path : 'modelset'                # Directory to save/load the model

prediction :
  charge : False                   # Predict atomic charges
  energy : True                   # Predict total energy
  force : True                    # Predict atomic forces

device :
  type : 'cuda'                   # Device to use: 'cuda' or 'cpu'
  number : 1                      # Specify CUDA device number (e.g., 0, 1)

data:
  charge : True                   # Whether the input data includes charge information
  e_field : False                 # Whether the input data includes external electric fields
  q_model : /home/work/gyrudgyrud21/CIGNN/modelset/q.pth.tar # Path to a pre-trained charge model (if any)
  
  site : /home/work/gyrudgyrud21/CIGNN_24/dataset/Figure/Figure_Submit/Table             # Path to the dataset directory
  test_data : ['re_test.npy']  # Filename(s) for the test dataset

save :
  best_save : ef.pth.tar        # Filename of the model to load for prediction